{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "terraveler:contracts:events:rank.changed:v1",
  "title": "rank.changed v1",
  "stream": "crew",
  "owner": "audit_log trigger on action 'crew-set-rank'",
  "type": "object",
  "invariants": "Manual rank change by the editor. When the Purser stands watch, automatic promotions/demotions will version this contract, not bypass it.",
  "required": [
    "action"
  ],
  "properties": {
    "submission_id": {
      "type": [
        "integer",
        "null"
      ]
    },
    "action": {
      "type": "string"
    },
    "verdict": {
      "type": [
        "string",
        "null"
      ]
    },
    "findings": {
      "type": [
        "array",
        "object",
        "null"
      ]
    }
  },
  "description": ""
}
