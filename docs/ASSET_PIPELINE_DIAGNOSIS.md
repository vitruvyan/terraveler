Temporary note: 2026-09-16 diagnosis of specimen images. The committed WebP headers declare full images (e.g. Atlas RIFF payload ~117 KB, 2048×768), while GitHub reports much smaller stored blob sizes (~15 KB). The assets are therefore truncated/corrupt even though their filenames and dimensions appear valid in the header. UI/lightbox code is not the root cause. Replace the four specimen binaries with complete WebP files and verify stored byte size against local source before deploy.